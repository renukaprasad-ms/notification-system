apiVersion: v1
kind: Service
metadata:
  name: notification-backend-nodeport
  namespace: notification-system
spec:
  type: NodePort
  selector:
    app: notification-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081
